{
  "name": "@elm-slate/test-command",
  "version": "1.0.3",
  "scripts": {
    "start": "elm make src/Main.elm --output elm.js && node main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "Unlicense",
"dependencies": {
    "@elm-slate/command": "elm-stuff/grove/packages/elm-slate/command/installed"
  }
}
